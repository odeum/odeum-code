import React from 'react'

const AppendixTable = (disableHeader,headerHeight,headerRowRenderer,
                        noRowsRenderer,rowRenderer,rowClicked,
                        rowHeight,rowGetter,rowCount,scrollToIndex,sort,sortBy,) => {
    return (
        <div>
            
        </div>
    )
}

export default AppendixTable